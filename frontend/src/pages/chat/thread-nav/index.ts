/**
 * Thread Navigator Module — Public API
 *
 * DeepSeek-style thread navigation for long chat conversations.
 * Standalone module, rendered at the ChatPage level.
 */

export { ThreadNavigator } from './components';
